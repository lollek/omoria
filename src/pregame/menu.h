#ifndef PREGAME_MENU_H
#define PREGAME_MENU_H

#include <stdbool.h>

/**
 * pregame_menu() - Run pregame menu
 *
 * @returns
 * * true if we should continue with the game
 * * false if we should quit the game
 */
bool pregame__menu(void);

#endif /* PREGAME_MENU_H */
