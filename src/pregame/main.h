#ifndef PREGAME_MAIN_H
#define PREGAME_MAIN_H

#include <stdbool.h>

/**
 * pregame_main() - Run pregame functions
 *
 * @returns
 * * true if we should continue with the game
 * * false if we should quit the game
 */
bool pregame__main(void);

#endif /* PREGAME_MAIN_H */

