#pragma once

#include <stdbool.h>

/**
 * init__stores() - Initialize store types
 *
 * @return - true on success, false of failure.
 * Will print information to stderr on failure
 */
bool init__stores(void);
