//! Trap templates.
//!
//! NOTE: This is scaffolding for Phase 2.1 (Trap Definitions). The real data
//! will be ported from `src/traps.c` in a later GREEN phase.

/// Trap templates from `traps.c` (placeholder).
///
/// In the C code, this corresponds to `trap_lista`.
pub static TRAP_LIST_A: &[()] = &[];

/// Trap templates from `traps.c` (placeholder).
///
/// In the C code, this corresponds to `trap_listb`.
pub static TRAP_LIST_B: &[()] = &[];
