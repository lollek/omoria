#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash, Serialize, Deserialize)]
pub enum RangedWeaponSubType {
    Shortbow,
    HuntersBow,
    CompositeBow,
    WarBow,
    DoubleBow,
    SiegeBow,
    WardedBow,
    SiegeCrossbow,
    Ballista,
    LightCrossbow,
    HeavyCrossbow,
    Sling,
}
